syntax = "proto3";

option go_package = "./clientStreaming";

package clientStreaming;

import "google/protobuf/wrappers.proto";

service ClientStreaming {
    rpc UploadMovie(stream A) return (google.protobuf.StringValue);
}

message A {
    int64 num = 1;
    bytes body =2;
}

